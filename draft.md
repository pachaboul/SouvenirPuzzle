- il permettre la selection de plusieurs imges (12 images maxi)
- si on a des images dans Mes Souvenirs, le choix des images doit etre aleatoires
- Une fois le match termine, il faut afficher le bouton Suivant (ca selectionner une autre image pour jouer) a cote de bouton Rejouer
- mettre le code sur github 
- mettre le apk telecharger par tout le monde sur le github 
…or create a new repository on the command line

echo "# SouvenirPuzzle" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/pachaboul/SouvenirPuzzle.git
git push -u origin main

…or push an existing repository from the command line

git remote add origin https://github.com/pachaboul/SouvenirPuzzle.git
git branch -M main
git push -u origin main




- Il faut ajouter le Menu HamburgurMenu autre pages exceptets la page (du match en cours)
- il faut ajouter le Bottom-Nav autre pages exceptets la page (du match en cours)
- A cote du button Creer un puzzle, il faut ajouter le Buttom Jouer (pour jouer et une images sera selectionner aleartoirement)

